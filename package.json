{
  "name": "konbini-kankoku-meshi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:all": "vite build && cd k-wave && npm run build && cd ../HanBok && npm run build && cd .. && node scripts/merge-dist.mjs"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.10"
  }
}
