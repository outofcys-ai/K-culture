# Context Notes

- 2026-06-30. 실제 실행 경로는 `src/main.jsx` -> `src/AppFinal.jsx` -> `src/data/content.js` 조합이다.
- 2026-06-30. `table.csv`는 12개의 추천 조합과 일본어/한국어 표기, 추천 이유만 제공하므로 가격, 조리 난이도, 태그는 화면 동작을 위해 최소한으로 보완한다.
- 2026-06-30. 이미지 파일은 루트의 `image` 폴더에 있으므로 데이터에서 `new URL("../../image/파일명", import.meta.url).href` 방식으로 연결한다.
- 2026-06-30. 카드 대표 이미지는 조합당 1장, 상세 장보기 목록은 가능한 재료별 썸네일을 함께 보여주도록 맞췄다.
- 2026-06-30. `npm run build`가 성공했고 이미지 자산도 함께 번들링되는 것을 확인했다.
- 2026-06-30. 사용자 요청에 따라 홈의 빠른 필터 영역은 제거하고, 상단 히어로 카드는 안내 문구와 버튼 없이 한 줄 제목만 남기기로 했다.
- 2026-06-30. 추가 요청에 따라 카드 설명, 상세 모달 설명/메모, 장보기 보조 설명, 추천 결과 설명은 노출하지 않도록 정리했다.
- 2026-06-30. 추가 요청에 따라 홈 화면의 추천 퀴즈 섹션은 제거하고 카드 목록만 남겼다.
- 2026-06-30. 추가 요청에 따라 지도 검색은 `brand/name/operator/branch`를 함께 정규화해 브랜드를 판별하고, 좌표가 거의 같은 중복 점포는 하나로 정리했다.
- 2026-06-30. 위치찾기 동작 안정화를 위해 HTTPS 필요 안내, 위치 권한 에러 메시지, 지도 `invalidateSize`, 권한이 이미 허용된 경우 자동 조회를 추가했다.
- 2026-06-30. localhost 개발 환경에서는 브라우저의 직접 외부 호출 대신 Vite 프록시 `/api/overpass`를 먼저 시도하도록 바꿨다.
- 2026-06-30. 한국어 구절 음성은 외부 TTS API 대신 기기 기본 `speechSynthesis`의 한국어 음성 중 점수가 높은 후보를 우선 선택하도록 구현했다.
- 2026-06-30. 시작 경험 요청에 맞춰 `src/main.jsx`가 가리키는 `src/AppFinal.jsx`만 유지하고, 별도 라우터 추가 없이 로컬 상태로 `landing -> village -> project` 3단계 화면 전환을 처리하기로 했다.
- 2026-06-30. 첨부된 궁궐/탐험 사랑방 이미지는 브라우저에서 안정적으로 로드되도록 `public` 폴더로 복사해 사용한다.
- 2026-06-30. 기본 `npm run build`는 기존 `dist/icon-192.svg` 파일 잠금으로 실패했지만, 같은 코드로 `npm run build -- --outDir dist-verify`를 실행해 최종 번들이 정상 생성되는 것을 확인했다.
- 2026-06-30. 모바일에서는 `object-fit: cover` 때문에 세로 화면에서 이미지 양옆이 과하게 잘리므로, `story` 화면만 모바일 분기에서 전체 그림이 더 많이 보이도록 레이아웃을 따로 보정하기로 했다.
- 2026-06-30. 추가 요청에 따라 `AppFinal.jsx`의 이미지 스토리 흐름을 `village`에서 여러 갈래로 확장하고, `K-POP 사랑방`, `동자승`, `명상` 이미지는 기존 방식과 동일하게 `public`으로 복사해 핫스팟 클릭으로 연결하기로 했다.
- 2026-06-30. `village` 화면에 `K-POP 사랑방`과 `문화사랑채` 핫스팟을 추가했고, `문화사랑채 -> 동자승 -> 명상` 단계는 별도 스토리 화면으로 이어지도록 `stage` 상태를 확장했다.
